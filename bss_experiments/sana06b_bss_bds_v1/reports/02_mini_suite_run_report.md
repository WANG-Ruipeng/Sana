# Sana-0.6B mini-suite run report

Status: not run in this local workspace.

The 16-prompt mini-suite must only run after smoke passes. It should use resume mode, one row at a time, dump runtime JSON and schedule JSON for every row, and sync completed artifacts to Drive if running in Colab.

Preferred method set:

- `uniform8`
- `uniform10`
- `uniform20`
- `uniform30`
- `uniform40`
- `reference_uniform50`
- `bss10`
- `bss20`
- `bss30`
- `bss40`

Completed prompt cases: 0.

Verdict: Need more data.
